apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: core-gateway
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: 'https://github.com/mmaous/mpes.git'
    targetRevision: main
    path: kubernetes/platform/ingress/core-gateway
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: istio-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - SkipDryRunOnMissingResource=true
