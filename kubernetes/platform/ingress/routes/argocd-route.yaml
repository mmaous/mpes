apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: argocd
spec:
  parentRefs:
    - name: mpes-gateway
      namespace: istio-system
  hostnames:
    - "argocd.mmlabs.me"
  rules:
    - backendRefs:
        - name: argocd-server
          port: 443
